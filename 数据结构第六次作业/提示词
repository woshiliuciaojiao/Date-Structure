提示词：写可运行的希尔排序 Python 代码，带分步打印过程、详细注释，同时通俗讲解排序原理、增量规则、复杂度及和直接插入排序的对比，要求最终呈现网络动画版本，要求生动形象
核心定位
希尔排序 = 升级版直接插入排序直接插入排序对乱序大数很慢，希尔排序先远距离分组、先粗略排有序，再逐步缩小间距，最后变成普通插入排序，整体大幅提速。
1. 核心思想
先取一个增量（间隔） gap，把数组按下标相隔 gap分成若干组；
对每一组内部单独做直接插入排序；
缩小增量 gap（一般折半），重复分组 + 组内插入排序；
直到 gap = 1，就是普通直接插入排序，此时数组已经接近有序，排序极快。
2. 增量常用规则
最常用：初始 gap = 数组长度//2，每次 gap = gap//2，直到 gap=0 停止。
3. 举个极简例子
数组：[9,5,1,4,3]
第一轮 gap=2：分成两组，组内插入排序，把大数先挪到后面；
第二轮 gap=1：变成普通插入排序，数组已经接近有序，几步就排好。
在演示中，有十组数据，gap为5，分成五组对比交换位置，再按照奇数和偶数分为两组排序，最后按照插入排序来排序获得最终排序结果。
4. 关键特点
为什么比普通插入快？
普通插入只能相邻交换，希尔排序远距离交换，大数一次性跳到后方，减少大量挪动次数。
时间复杂度：最坏 O(n 2)，平均 O(n 1.3)
空间复杂度：O(1)
 原地排序
不稳定排序（相同数值可能相对位置改变）
5. 和直接插入排序本质区别
直接插入：只能相邻逐个比对后移；
希尔排序：先跳着分组粗排，再精细全排，先宏观有序再微观微调。
python代码
def shell_sort(arr):
    arr = arr.copy()
    n = len(arr)
    # 初始增量
    gap = n // 2
    # 逐步缩小增量
    while gap > 0:
        print(f"\n当前增量 gap = {gap}")
        # 组内直接插入排序
        for i in range(gap, n):
            temp = arr[i]
            j = i
            # 向前跨gap位置比较后移
            while j >= gap and arr[j - gap] > temp:
                arr[j] = arr[j - gap]
                j -= gap
            arr[j] = temp
        print(f"gap={gap} 排序后数组：{arr}")
        gap = gap // 2
    return arr

# 测试
if __name__ == "__main__":
    test_arr = [9,5,1,4,3]
    print("原始数组：", test_arr)
    res = shell_sort(test_arr)
    print("\n最终有序数组：", res)
