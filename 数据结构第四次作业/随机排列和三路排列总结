随机快排对比标准快排的优化：
在标准快排的基础上，做了一个小改动，不再固定选最左 / 最右元素当 pivot，改为在当前区间里随机选一个 pivot，再交换到末尾，走标准分区流程
把数组分成两部分：≤ pivot 和 > pivot，得到 pivot 的位置后，递归排序左右两边
优点：实现简单，改动很小，避免了 “有序数组” 这种最坏情况，期望时间复杂度稳定在 O(n log n)，常数开销小，适合一般数据
缺点：对有大量重复元素的数组效果不好，本质还是两分区，没有针对重复元素做优化


三路快排：
专门为有大量重复元素的数组设计。把数组分成三部分：
< pivot （小于区）
== pivot （等于区）
> pivot （大于区）
这样处理后，中间等于 pivot 的部分，不用再递归排序了。
用三个指针 lt、i、gt：
lt：小于区的右边界
gt：大于区的左边界
i：当前遍历的元素
遇到：
arr[i] < pivot：和 arr[lt] 交换，lt 和 i 各 +1
arr[i] == pivot：i +1
arr[i] > pivot：和 arr[gt] 交换，gt -1（i 不变，因为换过来的元素还没检查）
分区完成后，只需要递归排序：
左边 < pivot 的部分
右边 > pivot 的部分
中间 == pivot 的部分直接跳过。
优点：对重复元素多的数组，效率极高，甚至接近线性时间，减少了不必要的递归和比较
缺点：实现比普通 / 随机快排稍微复杂一点，常数开销略大，对完全不重复的随机数据，和随机快排差不多甚至略慢
